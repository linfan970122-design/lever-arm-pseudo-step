# Analysis and figure scripts (any recent CPython 3.8+; developed on 3.12)
numpy>=1.20
pandas>=1.3
matplotlib>=3.4

# Lidar ICP only (analysis/lidar_icp_yaw.py, analysis/lidar_icp_yaw_windows.py).
# Pinned to the version used for the paper; it is the last release that installs
# cleanly next to a ROS Noetic / Python 3.8 environment.
# open3d==0.13.0

# Not installable with pip; needed only for the stages that read recordings or
# run the ROS replay:
#   ROS Noetic (rospy, rosbag)
#   robot_localization, branch noetic-devel, commit 040dd17
#   livox_ros_driver2 (message definitions for /livox/lidar)
